name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main
    paths:
      - index.html

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Build and Deploy
        run: |
          npm install
          npx gh-pages -d dist
